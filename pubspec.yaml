name: termex
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  dartssh2: ^2.17.1
  xterm: ^4.0.0
  flutter_pty: ^0.3.0
  uuid: ^4.0.0
  cryptography: ^2.7.0
  path_provider: ^2.0.0
  window_manager: ^0.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
